- name: Install ALSA
  xbps:
    package:
      - alsa-utils
    state: present

- name: Add user to audio group
  user:
    name: '{{ user | mandatory }}'
    groups:
      - audio

- name: Enable ALSA service
  runit:
    name: alsa
    enabled: yes
    state: started
